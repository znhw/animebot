{
  "name": "anime-chatbot",
  "version": "1.0.0",
  "description": "Anime chatbot with React frontend and Node.js backend",
  "scripts": {
    "build": "cd tweet-a-nime && npm install && npm run build"
  },
  "dependencies": {
    "@google/genai": "^1.31.0",
    "axios": "^1.13.2",
    "fuse.js": "^7.1.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}

